/*global define*/

define([
    'underscore',
    'backbone'
], function (_, Backbone) {
    'use strict';

    var CtaAlertModel = Backbone.Model.extend({
        defaults: {
        }
    });

    return CtaAlertModel;
});
