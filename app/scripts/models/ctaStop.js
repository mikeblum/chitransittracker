/*global define*/

define([
    'underscore',
    'backbone'
], function (_, Backbone) {
    'use strict';

    var CtaStop = Backbone.Model.extend({
        defaults: {
        }
    });

    return CtaStop;
});
